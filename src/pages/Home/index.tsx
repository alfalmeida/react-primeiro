import React from 'react'
import { useSelector } from 'react-redux';
import { rootStore } from '../../redux/store';

const Home = () => {
    

    return (
        <div>
            <h1>Home</h1>
        </div>
    )
}

export default Home;
